Task: {direction} this Markdown slide body to approximately {target_lines} lines (acceptable range {min_lines} to {max_lines}).
Return only the revised Markdown body, no fences.

SLIDE BODY:
{section}
